import RoadmapPlaceholder from "@/components/RoadmapPlaceholder";

export default function ForecastLabsPage() {
  return (
    <RoadmapPlaceholder
      breadcrumbLabel="Forecast & Labs"
      title="Forecast & Labs"
      description="Experimental: quantum threat timeline forecasting and what-if migration scenario modeling beyond the per-asset what-if already in Risk & Exposure."
      phaseLabel="Not yet built"
      explanation="Not scoped in any tracked phase yet — an experimental space for future work, not something planned for the SIH build."
    />
  );
}
